package com.timrobles.loader {	/**     * @author timrobles     */    public class LoadQueue implements ILoadQueue {    	    	private static var instance : LoadQueue;    	    	public function LoadQueue() {    	            //if ( arguments.callee != getInstance )                //throw new IllegalOperationError( getQualifiedClassName( this ) + " is a singleton and should only be accessed through getInstance()" );    		    	}    	    	public static function getInstance() : LoadQueue {    	            if ( instance == null )                instance = new LoadQueue();                     return instance;                   	    	}    	    	public function add( job : ILoadJob, priority : uint = 0 ) : void {    	    		job.load();	    		    	}    	    }    }